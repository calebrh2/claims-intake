**Current functionality**:
- Task service
- CRUD
- POST returns 500 error
- PUT and GET not implemented
- Delete uses legacy syntax + orders when there is no need (primary key already exists)
- Delete deletes the first id


**What needs fixing**:
- FUNCTIONALITY:
WI 01: POST tasks doesn't actually save to DB. Server returns 500.Incomplete fields are being returned as a task. Missing `completed` and `id`. Should implement with completed = FALSE since it's a new notification + id should be autogenerated by DB. 
WI 02: PUT not implemented. The endpoint should update the task based on what is in the body. Should only update the title, description, and completed, NOT ID
WI 03: GET with task_id as param not implemented. It should retrieve the corresponding task id from the db.
WI 04: Delete only deletes first task ordered by id ascending. Delete uses legacy syntax. Make more updated/less convoluded -> Doesn't need to order because 
WI 05: Error codes/ statuses are ambiguous. 
WI 06: no unit tests for the program
WI 07: Deleting already deleted task works -> Should not work


**Note**: SQLAlchemy is using outdated functions. The current version is 1.4.54. I am implementing the legacy version solutions, but this is considered tech debt. Whoever has authority over this next should update the version to 2.xx.

**Acceptance Criteria**:
AC-01: POST endpoint with request body of description + title actually creates the task, updates the database, and follows correct workflows
AC-02: GET /tasks/{task_id} actually retrieves the corresponding task from the database based on task_id
AC-03: PUT /tasks/{task_id} updates task description/title/completed


**Plan**:
1. Research SQLalchemy to find the correct flow, models, schemas, all with main.py
2. Fix functionality issues with POST, GET, and PUT. THIS IS THE MOST IMPORTANT PART BECAUSE THERE ARE ACTUAL FUNCTIONALITY GAPS.
3. Fix the issue that delete only deletes the first task id that is shown up.
4. Add proper error responses to all endpoints
5. Create Unit tests with mock data to test all endpoints + DB/ Connection stuff if I have the time

at the end write data contract and state what I own, what is still outstanding, etc.

Unit tests to write:
- Test all endpoints
- Successful post with valid fields should return 201
- Malformed Post should return 400 bad request
- Successful GET endpoint with valid type_id should return 200
- GET endpoint with type_id not found should return 404 not found
- Malformed GET request params should return 400 bad request
- Duplicate delete requests should return 404 not found

**Rules**:
- All endpoints should save to DB (WI-1)
- All endpoints should have logic (WI-2)
- Unit tests created for all endpoints (WI-7)
- Error codes are specific (WI - 8)
- Delete should only return 1 object anyway since task_id is primary key so there is no need to return first (WI - 04)
- Every task can only be deleted once (WI - 07)


**Important Note**:
- sqlAlchemy is using version 1.4.54. Should be changed to most recent version 2.xx. This prevents deprecation possibility. I made the previous db.query statements use the select() statements, which is compatible with the newest sqlalchemy orm version. All sqlAlchemy code should be updated to reflect the updated version of sqlAlchemy.


**What I own**:
- All endpoints are now successful with correct data.
- I still need to complete unit testing on different requirements.